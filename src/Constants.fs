module MatoFS.Constants

// Width of game area in cells
let gameWidth = 20

// Height of game area in cells
let gameHeight = 20

// Rendered width of cell in pixels
let width = 20

// Rendered height of cell in pixels
let height = 20

// How much the worm grows per food eaten
let growth = 5

// Initial length of the worm when the game starts
let initialLength = 5

// How many points are given per unit of worm length
let scorePerLength = 10